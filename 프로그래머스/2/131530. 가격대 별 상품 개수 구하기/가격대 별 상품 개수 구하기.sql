-- 코드를 입력하세요
SELECT
    DISTINCT(FLOOR(P.PRICE / 10000) * 10000) AS PRICE_GROUP,
    COUNT(FLOOR(P.PRICE / 10000) * 10000) AS PRODUCTS
FROM PRODUCT AS P

GROUP BY FLOOR(P.PRICE / 10000) * 10000
ORDER BY PRICE_GROUP ASC